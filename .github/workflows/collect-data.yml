name: Collect Brawl Stars Data

on:
  # Run daily at midnight CET (23:00 UTC in winter, 22:00 UTC in summer)
  # Note: GitHub Actions uses UTC, so we use 23:00 UTC which is midnight CET in winter
  schedule:
    - cron: '0 23 * * *'

  # Allow manual trigger
  workflow_dispatch:

  # Run on push to master (for testing)
  push:
    branches:
      - master
    paths:
      - 'collect_data.py'
      - 'src/**'
      - '.github/workflows/collect-data.yml'

jobs:
  collect:
    runs-on: ubuntu-latest
    permissions:
      contents: write  # Allow pushing commits

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Git Pull
        run: git pull

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install uv
        uses: astral-sh/setup-uv@v4

      - name: Install dependencies
        run: uv sync

      - name: Collect data
        env:
          BRAWL_STARS_PROXY_URL: ${{ secrets.BRAWL_STARS_PROXY_URL }}
        run: uv run collect_data.py

      - name: Commit and push changes
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add data/

          # Check if there are changes to commit
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Daily data update: $(date +'%Y-%m-%d')"
            git pull --rebase origin main
            git push
          fi
