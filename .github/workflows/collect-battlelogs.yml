name: Collect Battle Logs (DISABLED - Now running on Oracle VM)

on:
  # DISABLED: Battlelog collection moved to Oracle Cloud VM for reliable scheduling
  # Keeping workflow for reference and manual runs if needed
  # schedule:
  #   - cron: '*/30 * * * *'

  # Allow manual trigger (use only if Oracle VM collection fails)
  workflow_dispatch:

  # Run on push to main (for testing)
  push:
    branches:
      - main
    paths:
      - 'collect_battlelogs.py'
      - 'src/battle_store.py'
      - 'src/battle_models.py'
      - '.github/workflows/collect-battlelogs.yml'

jobs:
  collect:
    runs-on: ubuntu-latest
    permissions:
      contents: write  # Allow pushing commits

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install uv
        uses: astral-sh/setup-uv@v4

      - name: Install dependencies
        run: uv sync

      - name: Collect battle logs
        env:
          BRAWL_STARS_PROXY_URL: ${{ secrets.BRAWL_STARS_PROXY_URL }}
        run: uv run collect_battlelogs.py

      - name: Commit and push changes
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add data/battlelogs/

          # Check if there are changes to commit
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Battlelog update: $(TZ='Europe/Paris' date +'%A %Y-%m-%d %H:%M CET')"
            git push || (git pull --rebase origin main && git push)
          fi
